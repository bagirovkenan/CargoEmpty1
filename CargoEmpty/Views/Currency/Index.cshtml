@model IEnumerable<CargoEmpty.Models.General.Currency>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/AdminShared/_AdminLayout.cshtml";

    string isActive;
    string ActivClass;
}

<div class="col-lg-12 col-12  AminIndexPages" id="AdminNews" style="width:100%;height:auto;">

    <div class="row">
        <div class="col-lg-12 col-12">

            <table class="table table-bordered" style="width:100%;">
                <thead>
                    <tr>
                        <th scope="col">Currency Name</th>
                        <th scope="col">isActive</th>
                        <th scope="col">Editing</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {


                        if (item.IsActive)
                        {
                            isActive = "checked";
                            ActivClass = "Activ";
                        }
                        else
                        {
                            isActive = "";
                            ActivClass = "DeActiv";

                        }






                        <tr class="ss">
                            <td>@item.CurrencyName</td>
                            <td>
                                <div class="CheckBoxCurrency">
                                    <input type="checkbox" data-id="@item.Id" class="@ActivClass" id="@item.Id" name="IsActive" @isActive>
                                </div>
                            </td>
                            <td>
                                <button type="button" class="btn btn-info btn-sm CurrencyDetailsBtn" data-toggle="modal" data-target="#CurrencyDetailsModalLong" id="@item.Id">Details/Edit</button>
                                <button class="btn btn-danger btn-sm CurrencyDeleteBtn" data-toggle="modal" data-target="#CurrencyDeleteModal" id="@item.Id">Delete</button>
                            </td>
                        </tr>
                    }


                </tbody>
            </table>

        </div>
    </div>
    <button type="button" id="SaveActiveCurrencyBtn" class="btn btn-primary">Yadda saxla</button>
    <button type="button" class="btn btn-secondary" data-toggle="modal" data-target="#CurrencyCreateModal">Yenisini yarat</button>
    @Html.Partial("Create")

    @*  ========================================================================================*@
    <div class="modal fade" id="CurrencyDetailsModalLong" tabindex="-1" role="dialog" aria-labelledby="CurrencyDetailsModalLongLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="CurrencyDetailsModalLongLabel">About</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body" id="CurrencyeditPartialModal">
                </div>
            </div>
        </div>
    </div>

    @* ========================================================================================*@

    @*  ========================================================================================*@
    <div class="modal fade" id="CurrencyDeleteModal" tabindex="-1" role="dialog" aria-labelledby="CurrencyDeleteModalLabel" aria-hidden="true">

        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body" id="CurrencydeletePartialModal">

                </div>
            </div>
        </div>
    </div>

    @* ========================================================================================*@


</div>
@section CurrencyScript
{
    <script type="text/javascript" src="~/JavaScript/Currency.js"></script>

}

